const config = {
  activeDir: 'ltr',
  activeMode: 'light',
  activeTheme: 'LETIS_THEME',
  activeLayout: 'vertical',
  isLayout: 'boxed',

  isCollapse: 'full-sidebar', // always start expanded; user can toggle hamburger for mini
  isLanguage: 'en',
  isCardShadow: true,
  isMobileSidebar: false,
  isHorizontal: false,
  isBorderRadius: 10,    // more rounded — premium feel
  sidebarWidth: 258,     // slightly slimmer than 270
  miniSidebarWidth: 72,  // icon-only rail
  topbarHeight: 64,      // tighter than 70
};

export default config;
