import React from 'react';
import { Typography, Box } from '@mui/material';

const ThemeCustomizer: React.FC = () => (
  <Box p={3}>
    <Typography variant="h4">Theme Customizer</Typography>
  </Box>
);

export default ThemeCustomizer;
