import React from 'react';
import { Typography, Box } from '@mui/material';

const DomainManager: React.FC = () => (
  <Box p={3}>
    <Typography variant="h4">Domain Manager</Typography>
  </Box>
);

export default DomainManager;
