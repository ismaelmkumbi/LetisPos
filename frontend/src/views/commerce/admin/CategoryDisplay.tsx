import React from 'react';
import { Typography, Box } from '@mui/material';

const CategoryDisplay: React.FC = () => (
  <Box p={3}>
    <Typography variant="h4">Category Display</Typography>
  </Box>
);

export default CategoryDisplay;
