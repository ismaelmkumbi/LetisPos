import React from 'react';
import { Typography, Box } from '@mui/material';

const CommerceOrders: React.FC = () => (
  <Box p={3}>
    <Typography variant="h4">Commerce Orders</Typography>
  </Box>
);

export default CommerceOrders;
