import React from 'react';
import { Typography, Box } from '@mui/material';

const CommerceDashboard: React.FC = () => (
  <Box p={3}>
    <Typography variant="h4">Commerce Dashboard</Typography>
  </Box>
);

export default CommerceDashboard;
