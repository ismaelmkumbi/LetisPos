import React from 'react';
import { Typography, Box } from '@mui/material';

const NavigationBuilder: React.FC = () => (
  <Box p={3}>
    <Typography variant="h4">Navigation Builder</Typography>
  </Box>
);

export default NavigationBuilder;
