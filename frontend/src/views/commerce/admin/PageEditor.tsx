import React from 'react';
import { Typography, Box } from '@mui/material';

const PageEditor: React.FC = () => (
  <Box p={3}>
    <Typography variant="h4">Page Editor</Typography>
  </Box>
);

export default PageEditor;
